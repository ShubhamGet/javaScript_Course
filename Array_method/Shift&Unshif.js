// shift()
let num = [1, 2, 3, 4, 5];
let r = num.shift()// remove and element from the start of array
console.log(r, num);

// unshift
let r1 = num.unshift(10)// it add the element at the begining of the array and return new array length
console.log(r1, num);