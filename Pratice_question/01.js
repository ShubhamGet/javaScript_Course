//Q1. w.a.p to generate a array and take the input to the user and add it to the array

let arr = [2, 3, 4, 5];
let num = prompt("Enter a number ");
num = Number.parseInt(num);
arr.push(num);// used for insert number in Array
console.log(arr); 